[build]
  base = "apps/web"
  command = "cd ../.. && corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install && pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
